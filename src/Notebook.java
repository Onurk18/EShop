public class Notebook extends Product {


    public Notebook( int stock, int ram, int storage, double screen, double unitPrice, double discountRate, String name, Brand brand) {
        super( stock, ram, storage, screen, unitPrice, discountRate, name, brand);
    }
}
